# Quant Edge pipeline
# Python 3.11+.  Install:  pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
# PINNED, deliberately. data/model.pkl was pickled under 1.8.0 and carries
# sklearn._loss.* internals; loading it under 1.9.0 dies with
# "ModuleNotFoundError: No module named '_loss'". Unpinning means 04_screener.py
# cannot load the committed model, and regenerating the screener would require a
# retrain — which optuna's unseeded study makes non-reproducible. Bump only
# together with a fresh model.pkl.
scikit-learn==1.8.0

# Market data. Yahoo changes its endpoints without notice — if 01_data_pipeline.py
# returns empty frames, upgrade this first before debugging anything else.
yfinance>=0.2.40

# Required by pandas.read_html in get_sp500_tickers(). Without it the constituent
# fetch raises ImportError, the except branch returns an empty frame, and the
# pipeline finishes "successfully" with only SPY + the 12 sector ETFs — 13 tickers
# instead of 503. It was missing from this file, so a clean install reproduced that.
lxml>=5.0

# Hyperparameter search in 03_model.py. Note create_study() is called without a
# sampler seed, so best_params is not reproducible across runs.
optuna>=3.4

# datetime, json, logging, pickle, sqlite3, time and warnings are standard library.
# The dashboard loads its chart library from a CDN and needs network access.
